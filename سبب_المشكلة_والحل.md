# 🔧 السبب الحقيقي للمشكلة

## المشكلة:
الكود يحاول إنشاء profile للمستخدم الجديد، لكنه يفشل بسبب **نقص صلاحيات**.

## السبب:
الـ `SUPABASE_SERVICE_ROLE_KEY` غير موجود في ملف `.env.local`

هذا المفتاح ضروري لأنه يعطي الكود صلاحيات "المسؤول" لإنشاء الـ profiles تلقائياً.

---

## الحل (خطوتان فقط):

### 1️⃣ احصل على المفتاح

1. افتح https://app.supabase.com
2. اختر مشروعك
3. **Settings** → **API**
4. انسخ مفتاح **service_role** (وليس anon!)

### 2️⃣ أضفه للملف

افتح `.env.local` وأضف سطر جديد:

```env
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi... (المفتاح الطويل الذي نسخته)
```

يجب أن يصبح الملف هكذا:

```env
NEXT_PUBLIC_SUPABASE_URL=https://goyyfwrilfpvefmwlsjm.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_hCviG7HlVoyqLm8i4eJiiA_IM9shjIj
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJI... (المفتاح الكامل هنا)
```

### 3️⃣ أعد تشغيل الخادم

```bash
# في Terminal، اضغط Ctrl+C
# ثم شغل من جديد:
npm run dev
```

---

## ✅ النتيجة:

بعد إضافة المفتاح وإعادة التشغيل:
- التسجيل سيعمل بنجاح ✅
- الـ profile سيُنشأ تلقائياً ✅
- لن تظهر رسالة "Database error" ✅

---

## 📝 ملاحظات:

**لماذا نحتاج service_role key؟**
- لأن الـ RLS (Row Level Security) يمنع المستخدمين العاديين من إنشاء profiles
- الـ service_role يتجاوز هذه القيود ويسمح بالإنشاء التلقائي

**هل هذا آمن؟**
- نعم، لأن المفتاح يُستخدم فقط من السيرفر (Server Actions)
- لا يتم إرساله للمتصفح أبداً
- يجب عدم مشاركته مع أحد

---

## 🆘 إذا لم يعمل:

1. تأكد أن المفتاح نُسخ كاملاً (طويل جداً، 500+ حرف)
2. تأكد أنه على سطر واحد (بدون فواصل أسطر)
3. تأكد أنك أعدت تشغيل `npm run dev`
4. تحقق من Console في المتصفح لرسائل الخطأ

---

للمزيد من التفاصيل، راجع: `GET_SERVICE_KEY.txt`
